# OpenMW UI Tester
A quick, visual demonstration of various UI set ups to run through their properties and effects. Helps identify if there are regressions in UI code updates and changes.

## Installing
Add this repository folder (once cloned locally) into your OpenMW data directories and then enable the `omwscripts` file. As soon as you load a game the UI will appear. Use the arrow keys to cycle through examples.